<!-- res/menu/popup_menu.xml -->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/share"
        android:title="Share"
        android:icon="@drawable/share_button"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/add"
        android:title="Add_Note"
        android:icon="@drawable/add_button"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/check"
        android:icon="@drawable/check_note"
        android:title="Check Add Note"
        app:showAsAction="ifRoom" />
</menu>
